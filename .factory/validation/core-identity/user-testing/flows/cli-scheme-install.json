{
  "groupId": "cli-scheme-install",
  "surface": "file/metadata inspection",
  "toolsUsed": [
    "rg",
    "python3"
  ],
  "assertions": [
    {
      "id": "VAL-IDENT-002",
      "status": "pass",
      "reason": "Public CLI/install surfaces in the audited core-identity files now use the neozed binary name and neozed install/uninstall commands. Remaining legacy `zed` matches are limited to command-palette action labels or third-party/upstream identifiers outside the shell CLI scope.",
      "evidence": [
        {
          "kind": "file",
          "path": "crates/cli/src/main.rs",
          "summary": "The public CLI metadata declares `name = \"neozed\"` and advertises `neozed://` handling."
        },
        {
          "kind": "file",
          "path": "docs/src/reference/cli.md",
          "summary": "CLI reference installs `/usr/local/bin/neozed`, documents `neozed [OPTIONS] [PATHS]...`, and shows `neozed://` examples."
        },
        {
          "kind": "file",
          "path": "docs/src/macos.md",
          "summary": "macOS install docs now say the command palette creates a `neozed` command in `/usr/local/bin` and show only `neozed` shell examples."
        },
        {
          "kind": "file",
          "path": "docs/src/linux.md",
          "summary": "Linux uninstall guidance now uses `neozed --uninstall` and the corresponding `~/.local/.../neozed` fallback paths."
        },
        {
          "kind": "file",
          "path": "docs/src/uninstall.md",
          "summary": "The uninstall guide uses `brew uninstall --cask neozed` and `neozed --uninstall` commands only in the audited release-facing instructions."
        },
        {
          "kind": "command",
          "command": "python3 evidence generation for round-2-cli-surfaces.json",
          "summary": "Saved exact matched lines to evidence/core-identity/cli-scheme-install/round-2-cli-surfaces.json."
        }
      ]
    },
    {
      "id": "VAL-IDENT-005",
      "status": "pass",
      "reason": "The audited install and uninstall flows now use Neo Zed branding, neozed commands, dev.neozed identifiers, and neozed.dev/cloud.neozed.dev URLs. The remaining legacy `zed` socket cleanup path is an intentionally internal compatibility detail, not a user-facing install identity.",
      "evidence": [
        {
          "kind": "file",
          "path": "script/install.sh",
          "summary": "Install script comments and download URLs now point at `neozed.dev` and `cloud.neozed.dev` with `asset=neozed`, while install paths and symlinks use `neozed` and `dev.neozed`."
        },
        {
          "kind": "file",
          "path": "script/uninstall.sh",
          "summary": "Uninstall script messages, bundle IDs, app paths, and symlink removal all use Neo Zed/dev.neozed identities; only the internal socket cleanup keeps the compatibility-sensitive `zed-<channel>.sock` filename."
        },
        {
          "kind": "file",
          "path": "docs/src/macos.md",
          "summary": "macOS install guidance now points users to `https://neozed.dev/download` and Homebrew casks `neozed` / `neozed@preview`."
        },
        {
          "kind": "file",
          "path": "docs/src/linux.md",
          "summary": "Linux install docs now use the `https://neozed.dev/install.sh` installer and `cloud.neozed.dev` tarball URLs with `asset=neozed`, and uninstall instructions point to `neozed --uninstall`."
        },
        {
          "kind": "file",
          "path": "docs/src/uninstall.md",
          "summary": "The uninstall guide removes Neo Zed using `neozed` commands and Neo Zed file-system paths only."
        },
        {
          "kind": "command",
          "command": "python3 evidence generation for round-2-install-surfaces.json and round-2-scoped-legacy-audit.json",
          "summary": "Saved exact matched lines and scoped legacy-match audit to evidence/core-identity/cli-scheme-install/round-2-install-surfaces.json and round-2-scoped-legacy-audit.json."
        }
      ]
    }
  ],
  "frictions": [
    "Task-based user-testing-flow-validator subagents still failed to launch with \"Invalid model: custom:droidproxy:gpt-5.4\"; this rerun was completed manually in the validator session using the same assigned assertion scope and evidence outputs."
  ],
  "blockers": [],
  "notes": [
    "No services or seed data were required for this milestone.",
    "Remaining `zed` matches in the audited files are outside the release-facing CLI/install identity scope (command-palette action labels, third-party/upstream package identifiers, or the internal compatibility socket path)."
  ]
}
